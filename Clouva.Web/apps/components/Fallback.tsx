const Fallback = () => {
	return (
		<div className="flex h-screen items-center justify-center">
			<div className={`w-[2rem] h-[2rem] border-t-[0.3rem] border-b-[0.3rem] border-black rounded-full animate-spin`} />
		</div>
	);
};

export default Fallback;
